{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compileJava",
            "type": "shell",
            "command": "javac",
            "args": [
                "-d",
                "${fileDirname}",
                "${file}"
            ],
            "group": "build"
        }
    ]
}